<!--
Seed template for a consumer repository.
Insert the rendered block below into that repository's CLAUDE.md or AGENTS.md.
The referenced docs/agents files are created in the same approved setup write.
-->

## Agent skills

### Issue tracker

<One-line summary of where issues are tracked>. See `docs/agents/issue-tracker.md`.

### Triage labels

<One-line summary of the configured triage vocabulary>. See `docs/agents/triage-labels.md`.

### Domain docs

<One-line summary naming the single-context or multi-context layout>. See `docs/agents/domain.md`.
