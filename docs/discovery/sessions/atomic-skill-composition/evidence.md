# Evidence - Atomic Skill Composition

Every claim used in a decision must be traceable to an entry here.

## Sources

| Source | Read date | Revision | What it establishes | Node |
| --- | --- | --- | --- | --- |
| `AGENTS.md`, repository root | 2026-08-20 | sha256 `26e3594d6bb54a440813d2b927546e819efbf9365cfb38066c12d88201408530` | Canonical formats: skills at `skills/<name>/SKILL.md`; non-routable bases at `skills/_base/<name>/BASE.md` with no `SKILL.md`; `includes` is a dependency mirror; Markdown may not be replaced by generated manifests. | n-0002, n-0003 |
| `skills/` directory listing | 2026-08-20 | working tree at commit `e98a372` | 22 entries, of which `_base` is the only non-routable package directory. 21 routable skills plus one untracked `reinforce-skill`. | n-0004 |
| `skills/_base/chronicler/BASE.md` frontmatter | 2026-08-20 | commit `e98a372` | A base package declares `includes` and `requires-skills` and carries no `name`, `description`, or `allowed-tools`. It is therefore invisible to routing. | n-0003 |
| `skills/ship-with-squadron/SKILL.md` frontmatter | 2026-08-20 | commit `e98a372` | A routable skill declares `name`, `description`, `allowed-tools`, `includes`, and `requires-skills`. `requires-skills` entries carry `{id, source, required}` where `source` is `local` or `external`. This is the existing skill-to-skill dependency expression. | n-0002 |
| `docs/agents/domain.md` | 2026-08-20 | untracked working file | Single bounded context. Glossary at root `CONTEXT.md`; Architecture Decision Records at `docs/adr/`, both created lazily. | n-0001 |
| `docs/agents/issue-tracker.md` | 2026-08-20 | untracked working file | GitHub provider via `gh`, repository `jdylanmc/skills`. Discovery map and child ticket semantics with `discovery:*` labels. | n-0000 |
| Root `CONTEXT.md` | 2026-08-20 | commit `e98a372` | The repository already maintains an opinionated glossary under a "Skill orchestration" heading, currently holding Chronicler, Skill Run Log, Skill Run State, Operational Observation, and Reinforcement Evidence. | n-0001 |
| Discovery Loop reference `30-session-package-and-state.md` | 2026-08-20 | `~/.agents/skills/discovery-loop/` | "Tier" is already bound to the tracker hierarchy Branch, Story, and Task, including the `tracker-tier-map` frontmatter field and a `Tier` column. Establishes the naming collision recorded against `Composition level`. | n-0001 |
| Root `CONTEXT.md` after the c-0001 `/domain-mapping` handoff | 2026-08-20 | sha256 `d812205bfc2005abfae530f36fabf814ebf9cbc2a5c7c2acaeb4060fd5a91cca` | Canonical confirmed definition of `Atom`, written under a new `### Skill composition` subgroup. Source of truth for the confirmed mirror in `domain-model.md`. | n-0001 |
| `skills/domain-mapping/SKILL.md` and `references/40`, `references/50` | 2026-08-20 | commit `e98a372` | Contract verification for the c-0001 handoff: `/domain-mapping` accepts a material term, runs its own confirmation and Architecture Decision Record gates, writes only `CONTEXT.md`, `CONTEXT-MAP.md`, and approved records, and uses the compact bold-term glossary format with an optional discouraged-alias line. | n-0001 |
| `scripts/validate-skill-graph.mjs` | 2026-08-20 | commit `e98a372` | Line 202 excludes `_base` from routable skill discovery, so anything beneath `_base` is non-routable by shipped code. Line 277 forbids only `SKILL.md` under `_base`. `canonicalizeInclude` rejects includes that escape the skills root, including through a symlink, and rejects case or canonical path mismatches, so an includable unit must live under `skills/`. `requires-skills` entries must be objects with exactly `id`, `source`, and `required`. | n-0003, n-0005 |
| `scripts/conformance.test.mjs` | 2026-08-20 | commit `e98a372` | Line 281 already asserts `_base` never appears in the routable inventory. Non-routability for atoms and molecules is therefore already covered by an existing test. | n-0003 |
| `doctrine/manifest.md` and `AGENTS.md` Doctrine section | 2026-08-20 | commit `e98a372` | The repository already runs a generated-and-committed pattern: a manifest carrying a `schema-version` and a SHA-256 digest per file, regenerated by command after editing and verified before loading. This is the precedent cited for generated `used-by` and generated molecule `allowed-tools`. | n-0005 |
| `agents/skill-coach.agent.md` frontmatter | 2026-08-20 | commit `e98a372` | Flat single-file units already exist with `name`, `description`, `target`, `tools`, `disable-model-invocation`, and `user-invocable`. Routability control is prior art in this repository, so the atom model needed no new mechanism. | n-0003 |
| Root `CONTEXT.md` after the c-0002 `/domain-mapping` handoff | 2026-08-20 | sha256 `398c6907df230a1cc869e37c199bacc3fb88d1b1011d1964604c6aab4ea41759` | Canonical confirmed definition of `Molecule`, amended by the user to allow molecule-in-molecule composition. | n-0001 |
| Survey of all 21 routable skill packages | 2026-08-20 | working tree at commit `e98a372` | Every skill has a `references/` directory and a `## Required References` section - composition is already universal. **No routable skill has a `scripts/` directory**; `skills/_base/chronicler/` is the only script-bearing package in the repository. Only `ship-with-squadron` declares a non-empty `requires-skills`. | n-0001, n-0004, n-0006 |
| Reference-file classification scan | 2026-08-20 | working tree at commit `e98a372` | 165 reference files exist across the 21 skills. 135 contain no relative Markdown link, matching the confirmed Atom rule "references no other unit". 30 link to other Markdown units, which is molecule-shaped. The atom/molecule split already exists in the tree, unnamed, at a 135/30 ratio. | n-0001, n-0007 |
| `scripts/conformance.test.mjs` lines 298 and 385 | 2026-08-20 | commit `e98a372` | Both assert the include closure reaches `_base/chronicler/BASE.md`. Decomposing Chronicler changes that path, so both assertions are in the blast radius and are named in the acceptance criteria of Task #34. | n-0006 |
| Root `CONTEXT.md` after the c-0003 `/domain-mapping` handoff | 2026-08-20 | sha256 `e9b4fb986f490f59d6c414fd53e1a0bf48561ebe84cd988cb00e0ba483d2f760` | Canonical confirmed definition of `Skill`, including the user's framing "the contract the agent understands" and `organism` as a discouraged alias. All three composition levels are now confirmed. | n-0001 |
| GitHub issues #33 and #34 | 2026-08-20 | created in c-0003 | Branch and Task published from nodes n-0003 and n-0006, each carrying its immutable promotion key, with #34 wired as a native sub-issue of #33. | n-0003, n-0006 |

## Research Results

### c-0001 - Metaphor analysis of the proposed four-level scheme

Produced by the loop and presented to the user before Q5. Not a user decision
until the user accepted it.

- The proposed scheme `atom -> molecule -> ingredient -> recipe` mixes two
  metaphors. `Atom` and `molecule` are chemistry; `ingredient` and `recipe` are
  cooking.
- In the cooking metaphor an ingredient is the **primitive**. Placing it in the
  third slot inverts the ordering that the word itself implies, so a reader who
  knows what a recipe is will predict that ingredients are the bottom layer and
  be wrong.
- `Recipe` would name a unit this repository and the runtime already call a
  **skill** - `skills/`, `SKILL.md`, routing on `name` and `description`. A
  fourth word for an existing unit adds a synonym, not a distinction.
- Four levels requires four testable boundaries, while n-0001 had not yet
  produced one.

Outcome: the user accepted three levels, `atom -> molecule -> skill`, and kept
`recipe` as the mental model rather than a structural level.

### c-0001 - Atom boundary scenario stress-test

Run during the `/domain-mapping` handoff. Five scenarios; three passed cleanly.

| Scenario | Result |
| --- | --- |
| "Edit a file" - a thin wrapper over an existing tool | Atom. Consistent with the uniform bottom layer chosen in Q2. |
| "Ask one bounded question with a recommendation and an alternative" | Atom. One operation carrying an authored format. |
| "Query the database, then write results to a file" | Molecule. Two operations. |
| "Download a file" that internally performs authentication, retry, and checksum verification | **Ambiguous.** Resolved by ruling that a single operation is judged from the caller's point of view, so internal steps never split an atom. |
| `skills/_base/chronicler/` - build, validate, emit, replay, in a directory with 3 scripts and 2 tests | **Non-conforming.** Multiple operations, so not an atom; not a single file, so not a molecule under the c-0001 form rule. Created node n-0006. |

The stress-test also produced the mechanically checkable atom boundary now in
the confirmed definition: an atom references no other unit of composition, so
the existing dependency-graph validator can decide the question without human
judgment.

## Prototype Outputs

### c-0002 - Derived versus authored reverse links

The user asked whether a `used-by` reverse link could be kept correct by lint.
The loop had objected to `used-by` on three grounds; the analysis conceded one
and kept two.

| Objection | Survives lint? |
| --- | --- |
| The field goes stale silently | **No.** Conceded. `validate-skill-graph.mjs` already verifies that `includes` mirrors the real `## Required References` links, so a reverse-edge check is the same shape. A linted `used-by` is never wrong. |
| Fan-in churn | **Yes.** Adding one skill composing 8 atoms still means 9 file edits, and lint makes the 8 mandatory rather than optional. |
| Parallel-merge conflicts | **Yes, and worse.** `includes` is one-to-many written in the file being edited; `used-by` is many-to-one written into files the author is not editing. An atom used by 30 skills has a line every one of those 30 pull requests touches, and with lint a conflict becomes a build failure. |

The decisive observation: a linter able to *verify* a derived field is able to
*generate* it, from the same graph. The real choice was therefore
authored-and-verified versus generated-and-committed, and the latter delivers
bidirectionality with none of the surviving costs. The user chose it.

Precedent: `doctrine/manifest.md` commits SHA-256 digests, continuous
integration checks them, and `AGENTS.md` instructs regeneration by command.

No prototype has been proposed, approved, or run. This entry records analysis,
not execution.

## Limitations

- `docs/` is untracked in git at commit `e98a372`, so this discovery package is
  currently outside version control. Recorded at session setup on 2026-08-20.
- `skills/reinforce-skill/` is untracked and exists in no commit. It is counted
  in the 22 directory entries but is not part of the committed skill set.
